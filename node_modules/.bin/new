#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../_new@0.1.1@new/new.js" "$@"
  ret=$?
else 
  node  "$basedir/../_new@0.1.1@new/new.js" "$@"
  ret=$?
fi
exit $ret
