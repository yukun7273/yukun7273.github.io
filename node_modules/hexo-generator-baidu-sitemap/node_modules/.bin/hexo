#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../_hexo@3.9.0@hexo/bin/hexo" "$@"
  ret=$?
else 
  node  "$basedir/../../../_hexo@3.9.0@hexo/bin/hexo" "$@"
  ret=$?
fi
exit $ret
